# Assistara deployment source

The only deployable Assistara website source is `assistara-local-v9/`.

Vercel must use `assistara-local-v9` as its Root Directory. Do not create or edit duplicate website files at this repository root.
